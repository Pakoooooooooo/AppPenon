<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:fitsSystemWindows="true">

    <!-- Barre du haut -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <Button
            android:id="@+id/btnBack"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="← Retour"
            android:textSize="12sp"
            android:backgroundTint="@color/grey"
            android:textColor="@color/white" />

        <TextView
            android:id="@+id/tvGroupDetailName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Groupe"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="@color/sea"
            android:gravity="center"
            android:layout_marginHorizontal="8dp" />

        <Button
            android:id="@+id/btnEditGroup"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="⚙️"
            android:textSize="16sp"
            android:backgroundTint="@color/grey"
            android:textColor="@color/white" />

    </LinearLayout>

    <!-- État global du groupe -->
    <TextView
        android:id="@+id/tvGroupDetailState"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="8dp"
        android:text="Voile attachée"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="#4CAF50"
        android:gravity="center" />

    <!-- Séparateur -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#E0E0E0"
        android:layout_marginVertical="12dp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Pénons du groupe"
        android:textSize="14sp"
        android:textStyle="bold"
        android:textColor="@color/grey"
        android:layout_marginBottom="8dp" />

    <!-- Liste des pénons dans le groupe -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvGroupPenons"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

</LinearLayout>
